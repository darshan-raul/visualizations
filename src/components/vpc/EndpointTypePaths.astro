<figure class="endpoint-type-paths" aria-labelledby="endpoint-type-title">
  <figcaption id="endpoint-type-title"><span class="content-kicker">Private service paths</span><strong>Gateway and interface endpoints enter the route at different layers.</strong></figcaption>
  <div class="endpoint-type-grid">
    <section>
      <header><b>Gateway endpoint</b><span>S3 and DynamoDB</span></header>
      <div><strong>Workload</strong><i aria-hidden="true">route table</i><strong>Managed prefix list → endpoint</strong><i aria-hidden="true">private path</i><strong>AWS service</strong></div>
      <footer>No endpoint ENI · no endpoint security group · route-table association matters</footer>
    </section>
    <section>
      <header><b>Interface endpoint</b><span>PrivateLink service</span></header>
      <div><strong>Workload</strong><i aria-hidden="true">DNS</i><strong>Private endpoint ENI</strong><i aria-hidden="true">PrivateLink</i><strong>AWS/service endpoint</strong></div>
      <footer>Private IPs in selected subnets · endpoint security groups · private DNS may change resolution</footer>
    </section>
  </div>
</figure>

<style>
  .endpoint-type-paths { margin: 28px 0; }
  figcaption { display: grid; gap: 7px; margin-bottom: 16px; }
  figcaption strong { font-size: 1rem; }
  .endpoint-type-grid { display: grid; gap: 9px; }
  section { padding: 15px; border: 1px solid var(--border); border-radius: 9px; background: rgba(255,255,255,.014); }
  header { display: flex; justify-content: space-between; gap: 12px; }
  header b { font-size: .72rem; }
  header span, footer { color: var(--dim); font-family: var(--mono); font-size: .55rem; }
  section > div { display: grid; grid-template-columns: 1fr auto 1.3fr auto 1fr; align-items: center; gap: 7px; margin: 13px 0; }
  section strong { padding: 10px 7px; border: 1px solid var(--border); border-radius: 6px; background: var(--surface); font-size: .6rem; text-align: center; }
  section i { min-width: 60px; padding-top: 3px; border-top: 1px solid var(--green); color: var(--green); font-family: var(--mono); font-size: .48rem; font-style: normal; text-align: center; }
  @media (max-width: 650px) { header { flex-direction: column; } section > div { grid-template-columns: 1fr; } section i { justify-self: center; width: 60px; transform: rotate(90deg); } }
</style>
